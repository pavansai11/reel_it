# ReelMagic backend -- core runnable stack.
# This set runs the ENTIRE pipeline end-to-end (heuristic scoring fallback).
# For GPU-quality CLIP scoring + MediaPipe faces, also install requirements-ml.txt.

# Web / API
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.3

# DB
SQLAlchemy>=2.0

# Queue
redis>=5.0
rq>=1.16

# Media + scoring (lightweight)
pillow>=10.3
opencv-python-headless>=4.9
imagehash>=4.3
numpy>=1.26
librosa>=0.10
soundfile>=0.12
scenedetect>=0.6
ffmpeg-python>=0.2

# Edit brain
anthropic>=0.34
httpx>=0.27

# Storage (R2 / S3)
boto3>=1.34

# Config / utils
python-dotenv>=1.0
